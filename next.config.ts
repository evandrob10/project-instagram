import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['scontent-for2-1.cdninstagram.com','scontent-for2-2.cdninstagram.com']
  },
};

export default nextConfig;
